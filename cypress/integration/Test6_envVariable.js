describe("Working on checkbox", function () {
  it("Project No.2", function () {
    cy.visit(Cypress.env("url"));

    // Cypress.config("defaultCommandTimeout", 8000); --> will be applicable only after the wait specified line
  });
});
